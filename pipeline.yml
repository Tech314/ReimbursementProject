resources:
  - name: myGitRepo
    type: GitRepo
    configuration:
      gitProvider: aem_test
      path: Tech314/ReimbursementProject
      branches:
        include: master        
pipelines:  
  - name: basic_pipeline
    steps:
      - name: step_1
        type: Bash
        configuration:
          inputResources:
            - name: myGitRepo            
        execution:
          onExecute:
            - mvn clean install
